<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DevCache.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevCache.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="DevCacheUI">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="KeyTextBox" Width="150" PlaceholderText="Key" Margin="0,0,5,0"/>
            <TextBox x:Name="ValueTextBox" Width="250" PlaceholderText="Value" Margin="0,0,5,0"/>
            <Button Content="Set" Width="75" Click="SetButton_Click"/>
            <Button Content="Delete" Width="75" Margin="5,0,0,0" Click="DeleteButton_Click"/>
        </StackPanel>

        <controls:DataGrid x:Name="CacheDataGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Key" Binding="{Binding Key}" Width="*"/>
                <controls:DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="2*"/>
                <controls:DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <controls:DataGridTextColumn Header="TTL (s)" Binding="{Binding TtlSeconds}" Width="80"/>
                <controls:DataGridTextColumn Header="Size" Binding="{Binding SizeBytes}" Width="80"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Window>
